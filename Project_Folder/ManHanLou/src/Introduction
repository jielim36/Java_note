项目名：满汉楼（餐厅）
功能：订餐桌，点菜等功能。
技术栈: Java,MySQL数据库,JDBC

该项目主要是使用了分层设计模式。

第一层：View 界面层，调用service层的类，得到结果，显示数据
第二层：Service 服务层，完成业务，组织sql，并调用相应的DAO完成综合需求
第三层：DAO层，一个Java与数据库的中间层，用于处理各种数据，从数据库获取数据或从java发送数据到数据库。
第四层：Domain层，里面有每个数据库表对应的类。又叫JavaBean,pojo,domain
第五层：MySQL数据库，数据库，存放所有数据

软件分层是逻辑概念：
1.可以用不同的包来存放对应的层
2.Service层想要调用数据库的数据就需要通过Dao层，体现了一个调用的关系
3.每个包都各司其职
