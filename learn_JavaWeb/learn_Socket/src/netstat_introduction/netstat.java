package netstat_introduction;
/*
netstat指令

操作方法：
1.在cmd控制台输入netstat

介绍：
1.netstat -an 可以查看当前主机网络情况，包括端口监听情况和网络连接情况
2.netstat -an | more 可以分页显示
3.netstat -anb (需要以管理员身份运行cmd控制台) ：可以查看更详细的信息。比如：具体哪个应用程序在监听/使用该端口
4.当我们运行一个我们编写的Server时，会显示9999端口是java.exe正在使用的
 */
public class netstat {
}
